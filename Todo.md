Broken Pipes
Cloud
Panic

Game - Top down RPG
✅ - Fix player movement
✅ - Timer that starts at 30 seconds and goes down over time
✅ - Game panics as you get closer to the end = Timer + Shakes as the timer gets closer to zero

Basic Game loop:
- The third tile type is pipe (like in mario) add a fourth tile type = broken pipe
- Pipes break at random intervals over time
- The user fixes the pipes by going up to the breaks and pressing (E) to patch the broken pipe - and set it back to being a fixed pipe

Gameplay improvements:
- Each time you fix a pipe, the time goes back up
- High Score = number of pipes fixed
- Lose when timer runs out and the cloud has lost too many bits!


Styling:
- When they break, they leak bits (0s and 1s)
- Clouds connected by pipes

- make it look nicer (fix tiles with random grey bits on top)